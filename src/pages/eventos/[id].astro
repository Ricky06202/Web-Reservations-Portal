---
import ReservationsLayout from '@reservations/layout/ReservationsLayout.astro'
import ListaAsientos from '@reservations/components/ListaAsientos'

const { id } = Astro.params
---

<ReservationsLayout title='Lista de Asientos'>
	<ListaAsientos
		id={id}
		client:load
	/>
</ReservationsLayout>
